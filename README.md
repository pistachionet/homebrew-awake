# homebrew-awake

Homebrew tap for [Awake](https://github.com/pistachionet/awake), a menu bar app that
keeps a MacBook awake with the lid closed.

```sh
brew tap pistachionet/awake
brew install --cask awake
```

The cask installs a notarized build from the Awake GitHub releases.
